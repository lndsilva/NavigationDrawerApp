<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/main_item"
        android:title="Mail SubHeader Title">
        <menu>
            <item
                android:id="@+id/search_item"
                android:icon="@drawable/baseline_search"
                android:title="Search title"
                android:checkable="true"
                app:showAsAction="ifRoom"/>
            <item
                android:id="@+id/rotation_item"
                android:icon="@drawable/baseline_3d_rotation"
                android:title="3d title"
                android:checkable="true"
                app:showAsAction="ifRoom"/>
            <item
                android:id="@+id/accelerator_item"
                android:icon="@drawable/baseline_travel_explore"
                android:title="Travel Explore"
                android:checkable="true"
                app:showAsAction="ifRoom"/>
            <item
                android:id="@+id/dashboard_item"
                android:icon="@drawable/baseline_dashboard"
                android:title="Dashboard Title"
                android:checkable="true"
                app:showAsAction="ifRoom"/>
        </menu>
    </item>
    <item
        android:id="@+id/labels_item"
        android:title="SubHeader Title">
        <menu>
            <item
                android:id="@+id/label_one"
                android:icon="@drawable/baseline_label"
                android:title="Label one title"
                android:checkable="true"
                app:showAsAction="ifRoom"/>
            <item
                android:id="@+id/label_two"
                android:icon="@drawable/baseline_loop"
                android:title="Loop title"
                android:checkable="true"
                app:showAsAction="ifRoom"/>
        </menu>
    </item>
</menu>